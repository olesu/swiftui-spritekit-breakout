import Foundation

// TODO: PlaceHolder, not sure this is even a standalone class
final class DefaultLevelBricksProvider: LevelBricksProvider {
    func bricks(for level: LevelId) -> [BrickId: Brick] {
        [:]
    }
    
    
}
